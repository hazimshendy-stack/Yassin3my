// The ToastProvider lives in src/components/Toast.jsx and exposes useToast().
// This file re-exports it so consumers can do: import { useToast } from '@/hooks/useToast';
export { useToast } from '../components/Toast.jsx';
